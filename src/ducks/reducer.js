const initialState = {
    user: '',
    profilepic: '',
    cart: [],
    wishlist: []
}


export default function reducer(state=initialState, action) {
    switch(action.type) {
        case state:
            return state;
    }
}